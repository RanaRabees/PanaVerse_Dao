"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.formatWithPrettierIfAvailable = formatWithPrettierIfAvailable;

var _prettier = require("prettier");

async function formatWithPrettierIfAvailable(content) {
  const prettierConfig = await (0, _prettier.resolveConfig)(process.cwd());

  try {
    return (0, _prettier.format)(String(content), { ...prettierConfig,
      parser: "typescript"
    });
  } catch {
    return String(content);
  }
}