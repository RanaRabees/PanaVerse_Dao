"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractColorSchemeTypes = extractColorSchemeTypes;

var _isObject = require("../../utils/is-object");

const colorHueKeys = ["50", "100", "200", "300", "400", "500", "600", "700", "800", "900"];

function isColorHue(value) {
  if (!(0, _isObject.isObject)(value)) {
    return false;
  }

  const keys = Object.keys(value);
  return colorHueKeys.every(key => keys.includes(key));
}

function extractColorSchemeTypes(theme) {
  const {
    colors
  } = theme;

  if (!(0, _isObject.isObject)(colors)) {
    return [];
  }

  return Object.entries(colors).reduce((allDefs, [colorName, definition]) => {
    if (isColorHue(definition)) {
      allDefs.push(colorName);
    }

    return allDefs;
  }, []);
}