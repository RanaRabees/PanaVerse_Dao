"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.extractPropertyKeys = extractPropertyKeys;

var _isObject = require("../../utils/is-object");

function extractPropertyKeys(theme, themePropertyName) {
  const themeProperty = theme[themePropertyName];

  if (!(0, _isObject.isObject)(themeProperty)) {
    return [];
  }

  return Object.keys(themeProperty);
}