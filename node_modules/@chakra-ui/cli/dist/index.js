"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.run = run;

require("regenerator-runtime/runtime");

var path = _interopRequireWildcard(require("path"));

var _commander = require("commander");

var _chokidar = _interopRequireDefault(require("chokidar"));

var _lodash = _interopRequireDefault(require("lodash.throttle"));

var _initCli = require("./utils/init-cli");

var _tokens = require("./command/tokens");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _getRequireWildcardCache(nodeInterop) { if (typeof WeakMap !== "function") return null; var cacheBabelInterop = new WeakMap(); var cacheNodeInterop = new WeakMap(); return (_getRequireWildcardCache = function (nodeInterop) { return nodeInterop ? cacheNodeInterop : cacheBabelInterop; })(nodeInterop); }

function _interopRequireWildcard(obj, nodeInterop) { if (!nodeInterop && obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(nodeInterop); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

async function run() {
  await (0, _initCli.initCLI)();

  _commander.program.command("tokens <source>").option("--out <path>", `output file e.g. ${path.join(..._tokens.themeInterfaceDestination)}`).option("--strict-component-types", "Generate strict types for props variant and size").option("--no-format", "Disable auto formatting").option("--watch [path]", "Watch directory for changes and rebuild").option("--strict-token-types", "Generate strict types for theme tokens (e.g. color, spacing)").addOption(new _commander.Option("--template <template>", "Choose the template to use for the generation").default("default").choices(["default", "augmentation"])).action(async (themeFile, options) => {
    const {
      out,
      strictComponentTypes,
      format,
      strictTokenTypes,
      watch,
      template
    } = options;

    if (watch) {
      const watchPath = typeof watch === "string" ? watch : path.dirname(themeFile);
      const throttledGenerateThemeTypings = (0, _lodash.default)(async () => {
        console.time("Duration");
        await (0, _tokens.generateThemeTypings)({
          themeFile,
          out,
          strictComponentTypes,
          format,
          strictTokenTypes,
          template
        });
        console.timeEnd("Duration");
        console.info(new Date().toLocaleString());
      }, 1e3);
      throttledGenerateThemeTypings();

      _chokidar.default.watch(watchPath).on("change", throttledGenerateThemeTypings);

      return;
    }

    await (0, _tokens.generateThemeTypings)({
      themeFile,
      out,
      strictComponentTypes,
      format,
      strictTokenTypes,
      template,
      onError: () => process.exit(1)
    });
  });

  _commander.program.on("--help", () => {
    console.info(`Example call:
  $ chakra-cli tokens theme.ts
`);
  });

  _commander.program.parse();
}